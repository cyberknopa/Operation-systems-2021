Write a program for allocating memory and filling it with zeros in increments equal to the size of the memory page (mmap, VirtualAlloc). 
Get acquainted with the work of the OOM Killer daemon on Linux.
